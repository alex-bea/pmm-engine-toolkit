# Synthetic example

Request: activate `task-research`. The tracker shows the task in `todo`, its parent epic
exists, and no blocking dependency is recorded. Proposed transition: `todo -> active`.
Validation reruns after the write and the summary cites the task ID.
