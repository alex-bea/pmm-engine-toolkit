---
name: pmm-plan-scaffolder
description: Turn an accepted idea or proposal into a governed execution plan with tracker mapping. Use when the user asks to scaffold, formalize, or operationalize a plan; run PM Prioritizer first unless the user explicitly skips it.
---

# PMM Plan Scaffolder

1. Read `docs/STD-work-tracker-v1.0.md` and `docs/STD-approval-gates-v1.0.md`.
2. Use `$pm-prioritizer` for build-driving plans unless explicitly bypassed.
3. Follow `references/RUN-workflow.md` and render `assets/output-template.md`.
4. Use `scripts/scaffold_plan.py` for a deterministic starter file.
5. Consult `examples/EX-synthetic.md` for format only.

Draft before writing tracker state. Preserve `[Missing]` fields and ask for approval before local mutations.
