# Synthetic example

`/tmp/repo-feature-a` is clean and its branch is merged into main, so it is a candidate.
`/tmp/repo-feature-b` has local changes, so it is skipped. No removal occurs until the
candidate path is explicitly approved and rechecked.
