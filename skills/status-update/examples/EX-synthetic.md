# Synthetic example

Accomplished: approved onboarding positioning, supported by `weekly-notes`. Active: launch
checklist, supported by `tracker`. Blocker: analytics owner `[Missing]`. The same approval
mentioned in both sources appears once, with both source labels.
