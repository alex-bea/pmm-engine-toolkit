# Synthetic example

Northstar is adding reusable report filters for operations leads. Supplied beta notes show
that three teams recreated the same filters weekly. The brief labels adoption targets and
the public launch date `[Missing]` because neither appears in the source.
