# Synthetic example

Two approved local summaries cover the same onboarding decision. The promoter creates one
durable outcome, retains both source paths, keeps a dissenting measurement concern, and
writes only after the destination is confirmed.
