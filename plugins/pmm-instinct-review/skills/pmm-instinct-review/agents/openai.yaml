interface:
  display_name: "PMM Instinct Review"
  short_description: "Human-gated learning for Claude and Codex"
  default_prompt: "Use $pmm-instinct-review to identify the runtime, show its read-only status, verify Codex has an exact persisted extractor model, and explain the next exact human decision. If RUN or REF routing is ambiguous, show only validated eligible targets and require an exact selection. For a Claude installation, follow the bundled no-marketplace setup RUN."
policy:
  allow_implicit_invocation: true
